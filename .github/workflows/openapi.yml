name: OpenAPI
on:
  schedule:
    - cron: "0 0 * * 5"
  workflow_dispatch:
jobs:
  update-openapi:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust with rustfmt
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rustfmt

      - name: Run Codegen
        run: cargo run --release -- --fetch latest --emit-warnings
        working-directory: openapi

      - name: Check for crate inference warnings
        id: warnings
        run: |
          if [ -f openapi/out/crate_warnings.md ]; then
            echo "has_warnings=true" >> "$GITHUB_OUTPUT"
            {
              echo 'body<<WARNINGS_EOF'
              cat openapi/out/crate_warnings.md
              echo 'WARNINGS_EOF'
            } >> "$GITHUB_OUTPUT"
          else
            echo "has_warnings=false" >> "$GITHUB_OUTPUT"
          fi

      - name: create pull request
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: Generate latest changes from OpenApi spec
          title: Generate latest changes from OpenApi spec
          body: |
            This is an automated PR that tries to build the latest changes generated from the [Stripe OpenApi spec](https://github.com/stripe/openapi).

            ${{ steps.warnings.outputs.has_warnings == 'true' && steps.warnings.outputs.body || '' }}
          branch: openapi
          update-pull-request-title-and-body: true
